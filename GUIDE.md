# 🎮 特种兵前端改代码指南 (Beginner's Guide)

欢迎来到代码战场！这份指南会教你如何修改 `index.html`，把这个页面变成你专属的“战绩档案”。

## 🛠️ 准备工作
1. 确保你已经安装了 VS Code (或者记事本也能打开)。
2. 双击打开 `index.html` 就可以在浏览器预览效果。
3. 修改代码后，记得**保存 (Ctrl+S)**，然后刷新浏览器看效果。

---

## 🎯 任务 1：修改你的 ID 和头像
**目标**：把“特种兵_Alex”改成你的名字，换上你的帅气头像。

1. **改名字**：
   - 在代码里按 `Ctrl + F` 搜索：`特种兵_Alex`。
   - 找到第 **123行** 左右，把文字改成你的 ID。
   - 顺便把 `<title>和平精英特种兵档案...</title>` 里的名字也改了（在第 5 行）。

2. **改头像**：
   - 搜索：`img src="https://ui-avatars.com...` (约第 112 行)。
   - 把 `src="..."` 引号里的链接换成你自己的头像链接（可以用 Gitee 图片仓库链接，或者 QQ 头像链接）。

---

## 📊 任务 2：调整战绩数值 (雷达图)
**目标**：根据你的实际能力调整“五维图”。

- 搜索：`刚枪 (HTML/CSS)` (约第 166 行)。
- 你会看到两处数字：
  1. `<span>85%</span>` -> 这是显示给别人看的数字。
  2. `style="width: 85%"` -> 这是控制进度条长度的。
- **修改方法**：把两个 `85` 都改成你想要的数字（比如 `99`）。

---

## 📦 任务 3：上架你的“传说级装备” (项目展示)
**目标**：把你做过的网页或作品放上去。

- 搜索：`装备仓库` (约第 235 行)。
- 找到 `<a href="#" class="block group relative">...</a>` 这一大段代码。
- **改标题**：找到 `<h3>超酷个人博客</h3>`，改成你的项目名。
- **改描述**：找到 `<p>...</p>` 里的文字，写一段霸气的介绍。
- **改链接**：把 `href="#"` 中的 `#` 换成你项目的实际网址（比如 `https://yourname.gitee.io/project1`）。

---

## 🎨 进阶任务：修改主题色 (Tailwind CSS)
如果你想把“红绿配色”改成“蓝紫配色”：

- **红色**：代码里的 `bg-red-500` 或 `text-red-500`。试着改成 `bg-blue-500`。
- **黄色**：代码里的 `bg-yellow-500` (传说级颜色)。试着改成 `bg-purple-500`。
- **背景**：`bg-gray-900` 是深灰色背景，改成 `bg-black` 会变成纯黑。

---

## 🚀 部署到 Gitee Pages
1. 在 Gitee 上新建一个仓库（Repository）。
2. 把 `index.html` 上传到仓库。
3. 进入仓库 -> 服务 -> Gitee Pages。
4. 点击“启动”或“更新”。
5. 拿到你的专属网址，发给朋友炫耀吧！

---

**祝你代码落地成盒（划掉），大吉大利！** 🐔
